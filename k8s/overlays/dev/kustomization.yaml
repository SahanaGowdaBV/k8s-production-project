resources:
  - ../../base

namePrefix: dev-

commonLabels:
  env: dev

patches:
  - target:
      kind: Deployment
      name: devops-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
